export type globalStateType ={
    user:{
        cognitoId:string,
        createdAt:string,
        deletedAt:string,
        email:string,
        id:string,
        profileUrl: string,
        updatedAt:string,
        userName: string
    }
}